function testzz
    //usr/bin/env bun run (status current-filename) $argv; : '
 
(__ = (() => { 
    // const lodash = await import("/me/dev/abel")
    console.log("hello world", Bun.argv)
    
})()){ testzz() } ; //testzz()
/* '
end
