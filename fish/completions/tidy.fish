complete -c "tidy" -s "o" -l "output" -d "write output to the specified <file>" # global
complete -c "tidy" -l "config" -d "set configuration options from the specified <file>" # global
complete -c "tidy" -s "f" -l "file" -d "write errors and warnings to the specified <file>" # global
complete -c "tidy" -s "m" -l "modify" -d "modify the original input files" # global
complete -c "tidy" -s "i" -l "indent" -d "indent element content" # global
complete -c "tidy" -s "w" -l "wrap" -d "wrap text at the specified <column>" # global
complete -c "tidy" -s "u" -l "upper" -d "force tags to upper case" # global
complete -c "tidy" -s "c" -l "clean" -d "replace FONT, NOBR and CENTER tags with CSS" # global
complete -c "tidy" -s "b" -l "bare" -d "strip out smart quotes and em dashes, etc." # global
complete -c "tidy" -s "g" -l "gdoc" -d "produce clean version of html exported by Google Docs" # global
complete -c "tidy" -s "n" -l "numeric" -d "output numeric rather than named entities" # global
complete -c "tidy" -s "e" -l "errors" -d "show only errors and warnings" # global
complete -c "tidy" -s "q" -l "quiet" -d "suppress nonessential output" # global
complete -c "tidy" -l "omit" -d "omit optional start tags and end tags" # global
complete -c "tidy" -l "xml" -d "specify the input is well formed XML" # global
complete -c "tidy" -l "asxml" -d "convert HTML to well formed XHTML" # global
complete -c "tidy" -l "asxhtml" # global
complete -c "tidy" -l "ashtml" -d "force XHTML to well formed HTML" # global
complete -c "tidy" -l "access" -d "do additional accessibility checks" -xa "0 1 2 3" # global
complete -c "tidy" -l "raw" -d "output values above 127 without conversion to entities" # global
complete -c "tidy" -l "ascii" -d "use ISO-8859-1 for input, US-ASCII for output" # global
complete -c "tidy" -l "latin0" -d "use ISO-8859-15 for input, US-ASCII for output" # global
complete -c "tidy" -l "latin1" -d "use ISO-8859-1 for both input and output" # global
complete -c "tidy" -l "iso2022" -d "use ISO-2022 for both input and output" # global
complete -c "tidy" -l "utf8" -d "use UTF-8 for both input and output" # global
complete -c "tidy" -l "mac" -d "use MacRoman for input, US-ASCII for output" # global
complete -c "tidy" -l "win1252" -d "use Windows-1252 for input, US-ASCII for output" # global
complete -c "tidy" -l "ibm858" -d "use IBM-858 (CP850+Euro) for input, US-ASCII for output" # global
complete -c "tidy" -l "utf16le" -d "use UTF-16LE for both input and output" # global
complete -c "tidy" -l "utf16be" -d "use UTF-16BE for both input and output" # global
complete -c "tidy" -l "utf16" -d "use UTF-16 for both input and output" # global
complete -c "tidy" -l "big5" -d "use Big5 for both input and output" # global
complete -c "tidy" -l "shiftjis" -d "use Shift_JIS for both input and output" # global
complete -c "tidy" -s "v" -l "version" -d "show the version of Tidy" # global
complete -c "tidy" -s "h" -l "help" -d "list the command line options" # global
complete -c "tidy" -l "help-config" -d "list all configuration options" # global
complete -c "tidy" -l "help-env" -d "show information about the environment and runtime configuration" # global
complete -c "tidy" -l "show-config" -d "list the current configuration settings" # global
complete -c "tidy" -l "export-config" -d "list the current configuration settings, suitable for a config file" # global
complete -c "tidy" -l "export-default-config" -d "list the default configuration settings, suitable for a config file" # global
complete -c "tidy" -l "help-option" -d "show a description of the <option>" # global
complete -c "tidy" -l "language" -d "set Tidy's output language to <lang>" # global
complete -c "tidy" -l "xml-help" -d "list the command line options in XML format" # global
complete -c "tidy" -l "xml-config" -d "list all configuration options in XML format" # global
complete -c "tidy" -l "xml-strings" -d "output all of Tidy's strings in XML format" # global
complete -c "tidy" -l "xml-error-strings" -d "output error constants and strings in XML format" # global
complete -c "tidy" -l "xml-options-strings" -d "output option descriptions in XML format" # global