complete -c "lnx" -s "r" -l "real-path" -d "The target path the symlink will point to (real path)" # global
complete -c "lnx" -s "f" -l "fake-path" -d "The path where the symlink will be created (fake path)" # global
complete -c "lnx" -s "h" -l "help" -d "Show this help message" # global