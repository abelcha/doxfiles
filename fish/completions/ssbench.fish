complete -c "ssbench" -s "d" -l "directory" -d "Directory to measure, default is the current directory" # global
complete -c "ssbench" -l "bits" -d "Display results in bits per second instead of bytes per second" # global
complete -c "ssbench" -s "h" -l "help" -d "Print help" # global
complete -c "ssbench" -s "V" -l "version" -d "Print version" # global