complete -c "manx" -s "d" -l "direct" -d "Open the man page directly in your terminal window like usual, but with fancy colors." # global
complete -c "manx" -s "h" -l "html" -d "Format the man page as HTML, and open it in your default browser." # global
complete -c "manx" -l "browser" -d "Format the man page as HTML and open it in your default browser, or the app you pass with the --browser option." # global
complete -c "manx" -l "browser" -d "Format the man page as HTML and open it in your default browser, or the app you pass with the --browser option." # global
complete -c "manx" -s "p" -l "pdf" -d "Format the man page as PDF, and open it in your default PDF viewer." # global
complete -c "manx" -l "pdf-viewer" -d "Format the man page as PDF, and open it in your default PDF viewer, or the app you pass with the --pdf-viewer option." # global
complete -c "manx" -l "pdf-viewer" -d "Format the man page as PDF, and open it in your default PDF viewer, or the app you pass with the --pdf-viewer option." # global
complete -c "manx" -s "t" -l "text" -d "Format the man page as plain text, and open it in your default editor." # global
complete -c "manx" -l "editor" -d "Format the man page as plain text, and open it in your default editor, or the app you pass with the --editor option." # global
complete -c "manx" -l "editor" -d "Format the man page as plain text, and open it in your default editor, or the app you pass with the --editor option." # global
complete -c "manx" -s "x" -l "x-man-page" -d "Open the man page using macOS's x-man-page URL scheme." # global
complete -c "manx" -s "M" -l "manpath" -d "A colon-separated list of directories to search for man pages, instead of the default." # global
complete -c "manx" -s "S" -l "sections" -d "A manual section, or colon-separated list of sections to search." # global
complete -c "manx" -l "clear-cache" -d "Remove cached generated HTML/PDF/text files." # global
complete -c "manx" -l "completion" -d "Output shell script that implements command-line completion for Manx in zsh, then exit." # global
complete -c "manx" -l "help" -d "Display this usage information, then exit." # global