complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Clear" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Print" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Set" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Add" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Merge" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_use_subcommand" -a "Import" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Clear" -d "Clears out all existing entries, and creates root of specified Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Print" -d "Prints value of Entry or prints file if no entry is specified" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Set" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Add" -d "Adds Entry to the plist, with specified value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Merge" -d "Adds the contents of file.plist to specified Entry or the root if no Entry is specified" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Help'" -a "Import" -d "Creates or sets Entry with the contents of specified file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Clear" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Print" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Set" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Add" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Merge" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Exit'" -a "Import" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "ClearType" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "PrintEntry" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "SetEntryValue" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "AddEntryTypeValue" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "CopyEntrySrcEntryDst" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "DeleteEntry" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "MergefileplistEntry" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Save'" -a "ImportEntryfile" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "ClearType" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "PrintEntry" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "SetEntryValue" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "AddEntryTypeValue" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "CopyEntrySrcEntryDst" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "DeleteEntry" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "MergefileplistEntry" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Revert'" -a "ImportEntryfile" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "ClearType" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "PrintEntry" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "SetEntryValue" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "AddEntryTypeValue" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "CopyEntrySrcEntryDst" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "DeleteEntry" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "MergefileplistEntry" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Clear'" -a "ImportEntryfile" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Clear" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Print" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Set" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Add" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Merge" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Print'" -a "Import" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Clear" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Print" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Set" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Add" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Merge" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Set'" -a "Import" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "ClearType" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "PrintEntry" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "SetEntryValue" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "AddEntryTypeValue" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "CopyEntrySrcEntryDst" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "DeleteEntry" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "MergefileplistEntry" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Add'" -a "ImportEntryfile" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Clear" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Print" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Set" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Add" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Merge" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Delete'" -a "Import" -d "Creates or sets Entry the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Clear" -d "Clears out all existing entries, and creates root of a specified Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Print" -d "Prints the value of an Entry. Otherwise, prints the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Set" -d "Sets the value at Entry to the specified Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Add" -d "Adds Entry to the plist, with specified type and optional value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Merge" -d "Adds the contents of file.plist to Entry" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Merge'" -a "Import" -d "Creates or sets Entry to the contents of file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Help" -d "Prints this information" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Exit" -d "Exits the program, changes are not saved to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Save" -d "Saves the current changes to the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Revert" -d "Reloads the last saved version of the file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Clear" -d "Clears out all existing entries, and creates root of Type" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Print" -d "Prints value of Entry. Otherwise, prints file" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Set" -d "Sets the value at Entry to Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Add" -d "Adds Entry to the plist, with value Value" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Copy" -d "Copies the EntrySrc property to EntryDst" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Delete" -d "Deletes Entry from the plist" # sub
complete -f -c "PlistBuddy" -n "__fish_seen_subcommand_from 'Import'" -a "Merge" -d "Adds the contents of file.plist to Entry" # sub