complete -c "fzy" -s "l" -l "lines" -d "How many lines of items to display. If unspecified, defaults to 10 lines." # global
complete -c "fzy" -s "p" -l "prompt" -d "Input prompt (default: '> ')" # global
complete -c "fzy" -s "s" -l "show-scores" -d "Show the scores for each item." # global
complete -c "fzy" -s "t" -l "tty" -d "Use TTY instead of the default tty device (/dev/tty)." # global
complete -c "fzy" -s "q" -l "query" -d "Use QUERY as the initial search query." # global
complete -c "fzy" -s "e" -l "show-matches" -d "Non-interactive mode. Print the matches in sorted order for QUERY to stdout." # global
complete -c "fzy" -s "0" -l "read-null" -d "Read input delimited by ASCII NUL characters." # global
complete -c "fzy" -s "i" -l "show-info" -d "Show selection info line." # global
complete -c "fzy" -s "h" -l "help" -d "Usage help." # global
complete -c "fzy" -s "v" -l "version" -d "Usage help." # global